[Service]
# Service access is physical. Anyone who can reach this keyboard can already
# take the SD card out, so autologin grants nothing a locked console would
# withhold - and it means a fault can be diagnosed without a password that
# would otherwise have to live in a file.
ExecStart=
ExecStart=-/sbin/agetty --autologin kiosk --noclear %I $TERM
